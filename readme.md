# laravel Vue Mix

> Laravel Vue Mix

## Build Setup

``` bash
# install dependencies
$ npm run install

# update script
$ npm run dev

# update script with live changing
$ npm run watch

# build for production and launch server
$ npm run prod
$ npm run production

```
